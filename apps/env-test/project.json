{
  "name": "env-test",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/env-test/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/env-test"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nrwl/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "env-test:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "env-test:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "env-test:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nrwl/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "env-test:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "env-test:build:development"
        },
        "production": {
          "buildTarget": "env-test:build:production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/apps/env-test"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/env-test"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/env-test/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
